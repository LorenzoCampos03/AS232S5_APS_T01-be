package com.aps.hino.config;

/**
 * NOTA: Esta configuración de CORS ha sido movida a SecurityConfig.java
 * para mejor integración con Spring Security WebFlux.
 * 
 * Ver: SecurityConfig.corsConfigurationSource()
 */

// @Configuration - DESHABILITADO: CORS ahora se configura en SecurityConfig
public class CorsConfig {
    // Esta clase ya no se usa
    // CORS se configura directamente en SecurityConfig.java
}